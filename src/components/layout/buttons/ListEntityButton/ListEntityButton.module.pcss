.selectItemContainer {
	width: 100%;
	justify-content: space-between;
	align-items: center;
	flex-direction: row;

	.infoContainer {
		gap: var(--space-xs);
		flex-direction: row;
		align-items: center;
	}
}
