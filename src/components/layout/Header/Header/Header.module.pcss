.header {
	align-items: center;
	flex-direction: row;
	justify-content: space-between;
	padding: var(--st-br-sm) var(--space-xxl);
	background: var(--color-gray-25);
}

.comingSoonContainer {
	flex-direction: row;
}

.logoContainer {
	margin-right: 64px;
}

.separator {
	width: 1px;
	background: var(--color-gray-200);
	margin: auto var(--space-s);
	height: var(--space-m);
}

.navigatorContainer {
	flex-direction: row;
	align-items: center;
	gap: var(--space-s);
}

.headerButtonsContainer {
	flex-direction: row;
	align-items: center;
	gap: var(--space-s);
}

.headerButton {
	background: var(--color-gray-25) !important;
	padding: var(--space-xxs) var(--space-s) !important;
	color: var(--color-gray-600) !important;
	font-size: 1rem;
	font-weight: 500;
}

.headerButton:hover {
	background: var(--color-gray-25) !important;
	color: var(--color-gray-700) !important;
}

.headerButton:focus {
	box-shadow: 0px 0px 0px 4px rgba(11, 119, 177, 1) !important;
	box-shadow: 0px 0px 0px 2px var(--colorsneutralgray-25) !important;
}

.activeButton {
	color: var(--color-accent-600) !important;
}

.activeButton:hover,
.activeButton:focus {
	color: var(--color-accent-600) !important;
}

@media (max-width: 768px) {
	.header {
		padding: var(--st-br-sm) var(--st-br-lg);
	}

	.logoContainer {
		width: 90px;
		margin-right: 0;
	}
}
