.header {
	width: 100%;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	margin-bottom: var(--sp-sm);
}

.title {
	font-size: 1.125rem;
	color: var(--color-grey-600);
}

.value {
	font-size: 2.25rem;
	color: var(--color-grey-600);
	margin-bottom: var(--sp-sm);
}
