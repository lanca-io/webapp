.cardHeader {
	align-items: center;
	flex-direction: row;
	flex-wrap: wrap;
	gap: var(--space-xs);
	padding-bottom: var(--space-s);
}

.titleContainer {
	flex-direction: row;
	align-items: center;
	gap: var(--space-xs);
}
