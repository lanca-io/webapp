.container {
	gap: var(--sp-sm);
}

.type {
	font-size: 1rem;
	color: var(--color-grey-700);
	font-weight: 500;
}

.rowContainer {
	flex-direction: row;
	align-items: center;
	gap: var(--sp-xs);
}

.tagContainer {
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	gap: var(--sp-md);

	> p {
		width: 37px;
	}
}

.title {
	font-size: 14px;
	font-weight: 400;
}

.price {
	font-size: 1rem;
	font-weight: 500;
	color: var(--color-grey-700);
}

.symbol {
	color: var(--color-grey-700);
	font-weight: 500;
}

.chain {
	color: var(--color-grey-700);
	font-weight: 500;
}

.amountContainer {
	width: 100%;
	flex-direction: row;
	align-items: center;
	gap: var(--sp-sm);
	padding: var(--sp-sm) var(--sp-md);
}
