.wrapper {
	height: 100vh;
	width: 100%;
	display: flex;
	justify-content: center;
	align-items: center;
}

.banner {
	max-width: 498px;
	width: 100%;
	padding-top: 24px;
	padding-bottom: 24px;
	gap: var(--sp-sm);
	text-align: center;

	h3 {
		font-size: 24px;
		line-height: 28px;
		color: var(--color-grey-700);
	}

	p {
		font-size: 16px;
		color: var(--color-grey-600);
	}
}
