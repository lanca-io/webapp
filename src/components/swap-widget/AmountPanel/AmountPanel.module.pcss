.amount-panel {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: var(--sp-sm);
	align-self: stretch;
}

.enter-amount,
.error-message {
	display: flex;
	flex-direction: row;
	justify-content: center;
}

.error-message {
	font-size: 0.875rem;
	color: var(--color-danger-600);
}
