.selectTokenShape {
	align-items: flex-start;
	padding: var(--space-xxs);

	h4 {
		font-size: 1rem;
		color: var(--color-gray-600);
	}

	p {
		font-size: 0.875rem;
		color: var(--color-gray-500);
	}
}
