.container {
	gap: 64px;
}

.section {
	flex-direction: row;
	gap: var(--space-m);
}

.sectionHeading {
	color: var(--color-gray-600);
	font-weight: 500;
	font-size: 0.875rem;
}

@media (max-width: 1000px) {
	.section {
		margin: 0 auto;
		width: 100%;
		flex-direction: column;
	}

	.statSection {
		gap: var(--space-s);
	}
}

@media (max-width: 667px) {
	.statSection {
		flex-direction: column;
		gap: var(--space-s);
	}
}
