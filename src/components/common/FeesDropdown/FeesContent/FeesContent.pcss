.fees_content {
	display: flex;
	flex-direction: column;
	gap: var(--sp-sm);
}

.fees_content_heading {
	font-size: 1rem;
	color: var(--color-grey-100);
	font-weight: 500;
	line-height: 20px;
}

.fees_content_item {
	font-size: 0.875rem;
	font-weight: 400;
	line-height: 18px;
	color: var(--color-grey-300);
}
