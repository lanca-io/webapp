.destination_card_wrapper {
	position: relative;
	width: 100%;
	border-radius: var(--sp-xl);
	box-shadow: 0px 20px 150px 0px var(--color-accent-100);
}

.destination_card {
	display: flex;
	flex-direction: column;
	align-items: center;
	padding: var(--sp-xxl) var(--sp-lg) var(--sp-lg) var(--sp-lg);
	gap: var(--sp-md);
	align-self: stretch;
	border-radius: var(--sp-xl);
	background-color: var(--color-grey-25);
	position: relative;
	z-index: 1;
}
