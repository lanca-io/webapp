.area_chart {
	display: flex;
	flex-direction: column;
	min-height: 396px;
	align-items: flex-start;
	gap: var(--space-m);
	width: 100%;
	border-radius: 24px;
	background-color: var(--color-gray-25);
}
