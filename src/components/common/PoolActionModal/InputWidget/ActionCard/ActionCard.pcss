.pool_action_action_card {
	display: flex;
	flex-direction: column;
	padding: var(--space-m);
	align-items: flex-start;
	width: 100%;
	gap: calc(var(--space-s) - 2px);
	border-radius: 24px;
	background-color: var(--color-gray-25);
}
