.pool_action_input_form_header {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: var(--space-m);
	align-self: stretch;
	padding-right: 56px;
}

.pool_action_input_form_header_title {
	display: flex;
	align-items: flex-start;
	justify-content: center;
	gap: var(--space-xs);
	flex: 1 0 0;
	text-align: center;
	font-size: 1.125rem;
	font-weight: 500;
	line-height: 1.5rem;
	color: var(--color-gray-700);
}

@media only screen and (max-width: 640px) {
	.pool_action_input_form_header {
		position: relative;
		gap: 0;
		padding-right: 0;
		min-height: 48px;
	}
	.pool_action_input_form_header > *:first-child {
		position: absolute;
		left: 0;
		top: 50%;
		transform: translateY(-50%);
		z-index: 2;
	}
	.pool_action_input_form_header_title {
		margin: 0 auto;
		flex: unset;
		width: 100%;
		text-align: center;
		position: relative;
		z-index: 1;
		padding: 0 48px;
	}
}
