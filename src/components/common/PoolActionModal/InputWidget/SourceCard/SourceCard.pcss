.pool_action_source_card {
	display: flex;
	padding: var(--space-m);
	flex-direction: column;
	align-items: center;
	gap: var(--space-m);
	width: 100%;
	border-radius: 24px;
	background-color: var(--color-gray-25);
}
