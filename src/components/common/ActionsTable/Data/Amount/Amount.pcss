.actions_table_amount {
	display: flex;
	align-items: center;
	gap: var(--space-xxs);

	& .actions_table_amount_value {
		font-size: 0.875rem;
		line-height: 1.125rem;
		font-weight: 500;
		color: var(--color-gray-700);
	}

	& .actions_table_amount_denomination {
		font-size: 0.875rem;
		line-height: 1.125rem;
		font-weight: 500;
		color: var(--color-gray-500);
	}
}
