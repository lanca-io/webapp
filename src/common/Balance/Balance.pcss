.balance_container {
	display: flex;
	flex-direction: column;
	align-items: flex-end;
}

.balance {
	font-size: 1rem;
	font-weight: 500;
	color: var(--color-grey-700);
}

.balance_price_usd {
	font-size: 0.875rem;
	color: var(--color-grey-600);
}
