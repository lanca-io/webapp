.value_info_container {
	display: flex;
	flex-direction: row;
	justify-content: center;
	gap: var(--sp-xs);
}

.value_info {
	color: var(--color-grey-600);
	line-height: 18px;
	font-weight: 400;
	font-size: 0.875rem;
}
